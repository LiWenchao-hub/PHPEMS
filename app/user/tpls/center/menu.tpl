{x2;include:header_table}<style>    .a_user {        color: #ffffff    }    .a_user:hover {        color: rgba(5, 5, 5, 0.42)    }    .panel {        margin-bottom: 5px;    }</style><div class="panel panel-primary">    <div class="panel-heading">        <em class=""></em><span class="span_user">			{x2;if:$method == 'plane'}教学计划			{x2;else}			<a class="a_user" href="index.php?user-center-plane">教学计划</a>			{x2;endif}		</span>    </div></div><div class="panel panel-primary">    <div class="panel-heading">        <em></em><span class="span_user">		  {x2;if:$method == 'courses'}我的课程        {x2;else}			<a class="a_user" href="index.php?user-center-courses">我的课程</a>        {x2;endif}		</span>    </div></div><div class="panel panel-primary">    <div class="panel-heading">        <em></em><span class="span_user">		 {x2;if:$method == 'progress'}学习进度        {x2;else}		<a class="a_user" href="index.php?user-center-progress">学习进度</a>        {x2;endif}		</span>    </div></div><div class="panel panel-primary">    <div class="panel-heading">        <em></em><span class="span_user">		  {x2;if:$method == 'exams'}我的考场        {x2;else}		<a class="a_user" href="index.php?user-center-exams">我的考场</a>        {x2;endif}		</span>    </div></div><div class="panel panel-primary">    <div class="panel-heading">        <em></em><span class="span_user">		{x2;if:$method == 'search'}成绩查询		{x2;else}		<a class="a_user" href="index.php?user-center-search">成绩查询</a>		{x2;endif}		</span>    </div></div><div class="panel panel-primary">    <div class="panel-heading">        <em></em><span class="span_user">		  {x2;if:$method == 'privatement'}个人设置    {x2;else}    <a class="a_user" href="index.php?user-center-privatement">个人设置</a>    {x2;endif}		</span>    </div></div><!--<div class="panel panel-primary">    <div class="panel-heading">        <em></em><span class="span_user">	  {x2;if:$method == 'messages'}系统消息    {x2;else}    <a class="a_user" href="index.php?user-center-messages">系统消息</a>    {x2;endif}		</span>    </div></div>--><div class="panel panel-primary">    <div class="panel-heading">        <em></em><span id="span_user" class="span_user">	  {x2;if:$method == 'msg'}系统消息    {x2;else}    <a class="a_user" href="index.php?user-center-msg">系统消息</a>    {x2;endif}		</span>    </div></div><script type="text/javascript">    $(function () {        InitData();    });function InitData() {   var msgInfoData=getMsgInfo();   var total_flag=0;   // console.log(msgInfoData.length);   for(var i=0;i<msgInfoData.length;i++){       var theMsgTime=msgInfoData[i].msgtime;       // console.log(theMsgTime);       var systemTime = new Date().getTime();//获取当前系统时间       var thedatetime=new Date(theMsgTime).getTime();       var timeInterval=systemTime-thedatetime;//获得秒       // var day = parseInt(newtime / (1000 * 60 * 60 * 24));//得到天       // var day = parseInt(newtime / (1000 * 60 * 60 ));//得到小时       // var day = parseInt(newtime / ( 60  ));//得到分钟       var twoDays=1000 * 60 * 60 * 24*2;       if(parseInt(timeInterval)<twoDays){           total_flag++;       }   }   if(total_flag>0){       $("#span_user").append("<span id='span_user_dot' class='iconfont icon-dot' style='color:#ff0000;font-size: 12px;float: right'></span>")   }}function getMsgInfo(){    var msg_info="";    $.ajax({        url: "index.php?user-center-msg-msgInfoQuery", //请求的url地址        dataType: "json", //返回格式为json        async: false, //请求是否异步，默认为异步，这也是ajax重要特性        cache:false,        data: { "id": "value" }, //参数值        type: "POST", //请求方式        beforeSend: function() {            //请求前的处理        },        success: function(data) {            msg_info=data;        },        complete: function() {            //请求完成的处理        },        error: function() {            //请求出错处理        }    });    return msg_info;};</script>